@{
    ViewData["Title"] = "Hakkında";
}

<!-- Hero -->
<div class="container-fluid mb-5">
    <div class="p-5 rounded-4 shadow-sm hero-pink">
        <h1 class="fw-bold mb-3 text-dark">DeliverMate Lojistik</h1>
        <p class="fs-5 mb-0 col-lg-8 text-secondary">
            Teslimat süreçlerini merkezi, güvenli ve verimli bir şekilde yönetmek için
            geliştirilmiş modern bir lojistik yönetim platformu.
        </p>
    </div>
</div>

<div class="container">

    <!-- About -->
    <section class="mb-5">
        <h2 class="d-flex align-items-center mb-3">
            <i class="bi bi-info-circle me-2 pink-icon"></i>
            Proje Hakkında
        </h2>
        <hr />
        <p class="text-secondary col-lg-9">
            DeliverMate, <strong>ASP.NET MVC 8.0</strong> ve <strong>Entity Framework</strong>
            teknolojileri kullanılarak geliştirilmiş, teslimat operasyonlarını
            uçtan uca yönetmeyi amaçlayan kurumsal bir web uygulamasıdır.
        </p>
    </section>

    <!-- Features -->
    <section class="mb-5">
        <h2 class="d-flex align-items-center mb-3">
            <i class="bi bi-stars me-2 pink-icon"></i>
            Temel Özellikler
        </h2>
        <hr />

        <div class="row g-4">
            <div class="col-md-4">
                <div class="card h-100 border-0 shadow-sm feature-card">
                    <div class="card-body">
                        <i class="bi bi-shield-check fs-3 pink-icon"></i>
                        <h5 class="mt-3">Kimlik Doğrulama ve Yetkilendirme</h5>
                        <p class="text-muted mb-0">
                            Identity Framework ile rol bazlı erişim kontrolü
                            ve güvenli kullanıcı yönetimi.
                        </p>
                    </div>
                </div>
            </div>

            <div class="col-md-4">
                <div class="card h-100 border-0 shadow-sm feature-card">
                    <div class="card-body">
                        <i class="bi bi-arrow-repeat fs-3 pink-icon"></i>
                        <h5 class="mt-3">Kapsamlı CRUD İşlemleri</h5>
                        <p class="text-muted mb-0">
                            Teslimatlar, müşteriler, kargo kalemleri ve birimler
                            üzerinde tam yönetim yeteneği.
                        </p>
                    </div>
                </div>
            </div>

            <div class="col-md-4">
                <div class="card h-100 border-0 shadow-sm feature-card">
                    <div class="card-body">
                        <i class="bi bi-search fs-3 pink-icon"></i>
                        <h5 class="mt-3">Gelişmiş Arama</h5>
                        <p class="text-muted mb-0">
                            Kargo kalemi adı veya açıklamasına göre hızlı ve
                            esnek teslimat sorgulama.
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Usage -->
    <section class="mb-5">
        <h2 class="d-flex align-items-center mb-3">
            <i class="bi bi-person-workspace me-2 pink-icon"></i>
            Kullanım Bilgileri
        </h2>
        <hr />

        <div class="card border-0 shadow-sm usage-card">
            <div class="card-body">
                <h5 class="card-title mb-3">Demo Kullanıcısı</h5>
                <p class="text-muted">
                    Uygulamayı incelemek ve test etmek için aşağıdaki
                    demo kullanıcı bilgilerini kullanabilirsiniz.
                </p>

                <div class="row">
                    <div class="col-md-6 mb-2">
                        <span class="text-secondary">Kullanıcı Adı</span>
                        <div class="fw-semibold">
                            <i class="bi bi-envelope me-2 pink-icon"></i>
                            admin@gmail.com
                        </div>
                    </div>
                    <div class="col-md-6">
                        <span class="text-secondary">Şifre</span>
                        <div class="fw-semibold">
                            <i class="bi bi-key me-2 pink-icon"></i>
                            Admin123#
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

</div>

<!-- Custom Pink Theme -->
<style>
    :root {
        --pink-50: #fde4f2;
        --pink-100: #f9cee7;
        --pink-200: #f4b8da;
        --pink-300: #eea1cd;
        --pink-400: #e68bbe;
    }

    body {
        background-color: #ffffff;
    }

    .hero-pink {
        background: linear-gradient( 135deg, var(--pink-50), var(--pink-100), var(--pink-200) );
    }

    .pink-icon {
        color: var(--pink-400);
    }

    .feature-card {
        background-color: #ffffff;
        transition: transform 0.25s ease, box-shadow 0.25s ease;
    }

        .feature-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 .75rem 1.5rem rgba(230, 139, 190, 0.25);
        }

    .usage-card {
        background: linear-gradient( 180deg, #ffffff, var(--pink-50) );
    }

    hr {
        border-top: 1px solid var(--pink-100);
        opacity: 1;
    }
</style>
