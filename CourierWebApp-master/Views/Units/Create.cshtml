@model CourierWebApp.Models.Units

@{
    ViewData["Title"] = "Oluştur";
}

<h1>Oluştur</h1>

<h4>Birim</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group mb-3">
                <label asp-for="Unit" class="control-label">Birim</label>
                <input asp-for="Unit"
                       class="form-control"
                       placeholder="adet, kg, m, litre" />
                <span asp-validation-for="Unit" class="text-danger"></span>
            </div>

            <div class="form-group">
                <button type="submit"
                        class="btn text-white d-flex align-items-center"
                        style="background-color: #e68bbe; padding: 0.5rem 1rem;">
                    <i class="bi bi-rulers me-2"></i> Oluştur
                </button>
            </div>
        </form>
    </div>
</div>

<div class="mt-3">
    <a asp-action="Index"
       class="btn text-white d-flex align-items-center"
       style="background-color: #f4b8da; padding: 0.5rem 1rem; width: fit-content;">
        <i class="bi bi-arrow-left me-2"></i> Listeye Dön
    </a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}